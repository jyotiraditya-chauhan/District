//
//  AppColors.swift
//  District
//
//  Dark-first premium palette. District does not support a light appearance —
//  colors are fixed, not adaptive, per the app's design system.
//

import SwiftUI

enum AppColors {
    static let background = Color(hex: "#131315")
    static let surface = Color(hex: "#1D1D1F")
    static let card = Color(hex: "#2C2C2E")
    static let divider = Color(hex: "#9CA29D")
    static let primaryText = Color(hex: "#F5F5F7")
    static let secondaryText = Color(hex: "#A1A1A8")
    static let accent = Color(hex: "#4F972E")
    static let ctaBackground = Color(hex: "#F4F4F4")
    static let ctaText = Color(hex: "#141414")
}
